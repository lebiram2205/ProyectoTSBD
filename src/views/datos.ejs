<%- include('partials/header') %>
<%- include('partials/contenidoInformativo') %>

<div class="container">
    <div class="row mt-5">
        <div class="col-md-7">
        <table class="table tlable-botdered table-hover">
            <thead>
            <tr>

            </tr>
                <th>numero</th>
                <th>Nombre</th>
                <th>Sexo</th>
                <th>Acciones</th>
            </thead>
            <tbody>
                <% if (data) { %>
                    <% for(var i = 0; i < data.length; i++) { %>   
                        <tr>
                            <td><%= (i + 1) %></td>
                            <td><%= data[i].nombre %></td>
                            <td><%= data[i].sexo %></td>
                            <td>
                                <a href="/actualizar/<%=data[i].nombre%>" class="btn btn-info">actualizar</a>
                                <a href="/eliminar/<%=data[i].id_DUsuario%>" class="btn btn-danger">Eliminar</a>
                            </td>
                        </tr>
                        <% } %>
                      <% } %>
               
            </tbody>
        </table>

        </div>
        <div class="col-md-5">
            <form action="/add" method="post">
                <div class="form-group">
                    <input type="text" name="nombre" placeholder="Name" class="form-control" autofocus>
                </div>
                <div class="form-group">
                    <input type="text" name="sexo" placeholder="Sexo" class="form-control">
                </div>
             <button type="submit" class="btn btn-primary">Terminar Formulario</button>

            </form>
        </div>

    </div>

</div>
<pre><%=data[0].id_DUsuario%></pre>

<div class="container">
</div>


<%- include('partials/footer') %>






